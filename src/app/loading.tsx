import React from "react";
import LoadingIcon from "@/components/loading/LoadingIcon";

function loading() {
    return (
        <div>
            <LoadingIcon />
        </div>
    );
}

export default loading;
